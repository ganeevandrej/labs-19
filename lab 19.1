# include <iostream>

using namespace std;

int main()
{
    int a[10];
    int n;

    cin >> n;

    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    int k = 0;
    for (int i = 1; i < n; i++) {
        if (a[k] != a[i])
        {
            ++k;
            a[k] = a[i];
        }
    }
    ++k;
    for (int i = 0; i < k; i++)
        cout << a[i] << endl;
    return 0;
}
